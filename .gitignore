# OS / system files
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
*.lnk

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Node
node_modules/
*.log
npm-debug.log*

# Env files — NEVER commit real secrets
.env
.env.local
.env.*.local
.vercel
# (.env.example is intentionally tracked as documentation)

# Build / cache
dist/
build/
public/
.cache/
.tmp/

# Local Claude Code config (machine-specific)
.claude/

# Source materials (personal photos, biographical docs, drafts) —
# kept locally only; the website uses the optimized copies in /assets
данные для создания сайта/
