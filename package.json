{
  "name": "morodenko-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Сайт психолога Мороденко Евгении · кандидат психологических наук, доцент",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:css": "lightningcss styles.css --minify --targets \">= 0.25%\" --output-file styles.min.css",
    "build:js": "terser script.js --compress --mangle --output script.min.js",
    "build:public": "node scripts/build-public.mjs",
    "build": "npm run build:css && npm run build:js && npm run build:public",
    "check": "node --check script.js && node --check script.min.js && node --check api/send-lead.js"
  },
  "dependencies": {
    "resend": "^4.8.0"
  },
  "devDependencies": {
    "lightningcss-cli": "1.32.0",
    "terser": "5.46.2"
  }
}
